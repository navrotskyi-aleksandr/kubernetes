apiVersion: batch/v1
kind: CronJob
metadata:
  name: helloworld
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: hello
            image: busybox:1.28
            imagePullPolicy: IfNotPresent
            command:
            - /bin/sh
            - -c
            - date; echo Hello  Kubernetes cluster
          restartPolicy: OnFailure